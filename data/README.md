# Data Workspace

This directory keeps only repository-safe placeholders and templates.

- `Acquire/`: temporary acquisition workspace, local-only data
- `Discover/`: search batches and review artifacts, local-only data
- `Library/`: organized paper library, local-only data
- `templates/`: reusable tracked templates

Real data files in this tree are ignored by Git by default.
