name: paper-note-generate
version: 1
interfaces:
  short_description: "Generate deep paper notes with resolved figure/table evidence"
  default_prompt: "Use $paper-note-generate when improving Research-Flow paper note prompts, managed note blocks, or image-backed note rendering."
