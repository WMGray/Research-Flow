interface:
  display_name: "Paper Refine Parse"
  short_description: "MinerU full.md refinement, figure/table-aware repair, and section split rules"
  default_prompt: "Use $paper-refine-parse to improve MinerU full.md refinement, canonical section splitting, and verification without rewriting the full paper."
