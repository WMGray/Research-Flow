# 参考资料

本目录保存不会直接成为产品需求或后端契约、但会影响设计判断的参考材料。

当前内容：

- `claude-scholar分析.md`：外部竞品或参考项目分析。
- `frontend/screenshots/`：前端参考截图。
- `frontend/html/`：前端参考 HTML 原型。

约束：

- 参考素材可以辅助设计，但不能替代 `00_overview`、`02_backend`、`03_api` 中的正式需求与接口契约。
- 测试报告、联网探针报告和一次性审计记录不放在这里，统一放入 `reports/`。
