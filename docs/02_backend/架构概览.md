# Research-Flow 后端架构概览

## 1. 文档目标

本文档用于定义 Research-Flow 后端的总体技术架构、模块边界、存储策略、任务编排方式与外部服务接入原则，作为后续模块设计、数据库设计、接口细化与代码实现的统一依据。

本文档回答的问题是：

- 后端采用什么技术基线
- 系统如何按层划分
- `Paper`、`Project`、`Knowledge`、`Dataset`、`Presentation` 如何协同
- 文件系统与 SQLite 如何分工
- 异步任务如何支持并发执行与串行依赖
- LLM / Agent 如何通过通用 provider 抽象接入
- 当前首版哪些能力优先实现，哪些顺序后置

本文档不展开：

- 前端页面与交互细节
- 具体数据库字段级设计
- 单个接口的请求响应参数细节
- 单个第三方服务的接入配置说明

---

## 2. 当前架构基线

### 2.1 技术栈

当前后端技术方案采用以下基线：

- Web 框架：`FastAPI`
- Agent / LLM orchestration：`Agno`
- 本地数据库：`SQLite`
- 核心存储：本地文件系统

### 2.2 技术选型原则

- 优先保证单机、本地优先、可快速迭代
- 先打通从论文入库到课题沉淀的完整链路
- 保留对多 provider LLM 的通用抽象，不把业务逻辑绑死在单一平台
- 对未来任务并发、provider 扩展、数据库迁移保留升级空间

### 2.3 当前阶段边界

当前首版以“平台主链路可跑通”为首要目标：

- 除 `Zotero` 外，`Paper`、`Project`、`Knowledge`、`Dataset`、`Presentation`、每日推荐、会议追踪、投稿/Rebuttal、录用后处理都进入首版实现范围
- 不同能力按 `P0 / P1 / P2` 区分优先级、实现顺序与完成度要求
- `Presentation`、每日推荐、会议追踪、投稿/Rebuttal、录用后处理优先级较低，但不是只保留接口或模型骨架
- Zotero 集成明确延后

---

## 3. 系统定位与核心对象

系统围绕五类核心资源组织：

- `Paper`：论文入库、解析、分节、分析与 `note` 生成的核心起点
- `Project`：围绕研究问题沉淀长期材料与六模块工作区
- `Knowledge`：从论文中提炼出的结构化观点、定义、特征
- `Dataset`：论文与实验中涉及的数据集档案
- `Presentation`：组会汇报与阶段性输出资源

资源优先级为：

`Paper > Project > Presentation > Knowledge > Dataset`

设计原则是：

- 资源中心化：所有生成、抽取、分析能力都挂载在资源上
- 本地优先：正文、PDF、Markdown、导出物进入文件系统
- 数据索引化：SQLite 保存元数据、关系、状态、配置与任务
- 流程任务化：下载、解析、优化、生成等都以 `Job` 表示

---

## 4. 总体分层架构

后端采用分层架构，建议拆分为五层。

### 4.1 API 层

职责：

- 暴露 REST API
- 做输入校验、权限边界与响应封装
- 创建任务、查询资源、返回状态

不负责：

- 直接执行业务流程
- 直接调用 provider
- 直接写复杂文件编排逻辑

### 4.2 Application / Service 层

职责：

- 编排核心业务流程
- 决定资源状态流转
- 决定任务依赖与任务触发时机
- 协调数据库、文件系统、provider 与任务系统

典型服务：

- `PaperService`
- `ProjectService`
- `KnowledgeExtractionService`
- `DatasetService`
- `PresentationService`
- `JobService`
- `ConfigService`

### 4.3 Domain / Workflow 层

职责：

- 表达核心业务规则与流程约束
- 定义单个资源的状态机
- 定义任务依赖、人工审查、增量更新策略

典型对象：

- `PaperWorkflow`
- `ProjectWorkflow`
- `JobDependencyPolicy`
- `NoteMergePolicy`
- `ReviewCheckpointPolicy`

### 4.4 Repository / Storage 层

职责：

- 封装对 `SQLite` 的访问
- 封装对本地文件系统的访问
- 保证数据库记录与文件路径映射一致

典型组件：

- `PaperRepository`
- `ProjectRepository`
- `JobRepository`
- `AssetRepository`
- `FileStorage`

`SQLite` 部分采用与表设计一致的四段式思路：

- 物理层：记录真实文件与目录位置
- 资产层：为文件、目录与逻辑对象提供统一资产 ID
- 关系层：显式记录资源之间的关联关系
- 业务层：承载 `Paper`、`Project`、`Knowledge`、`Dataset`、`Presentation` 的领域字段

这样做的好处是：

- 文件系统与数据库可解耦演进
- 同一份物理文件可以被逻辑资源引用
- 后续知识图谱、导出物追溯、任务产物归属都有统一挂载点

### 4.5 Integration 层

职责：

- 对接 PDF parser、LLM provider、Agno agent、云服务与未来的 Zotero
- 将不同 provider 统一成稳定协议

典型组件：

- `PdfParserAdapter`
- `LlmProvider`
- `AgentRunner`
- `ProviderRegistry`

---

## 5. 目录与存储布局

### 5.1 总体原则

- 文件系统保存重内容与产物
- SQLite 保存轻量索引、关系、状态与配置
- 每个资源都必须能同时在数据库中被索引、在文件系统中被追溯
- 资源目录统一采用 slug 化命名，展示名与目录名分离

### 5.1.1 slug 规则

为了保证跨平台可移植性与路径稳定性，`PaperName`、`ProjectName`、分类路径都采用 slug 化目录名。

建议：

- 数据库存展示原始标题和原始名称
- 文件系统使用 `direction_slug`、`sub_direction_slug`、`paper_slug`、`project_slug`
- slug 生成后尽量稳定，不随轻微标题调整频繁变化
- 需要保留重命名能力，但应通过迁移逻辑统一更新数据库映射

### 5.2 Paper 目录布局

论文统一落在 `Papers/` 顶层目录下，按“大方向 / 小方向 / PaperName” 组织，落盘时使用 slug 化路径。

建议目录结构：

```text
Papers/
  {direction_slug}/
    {sub_direction_slug}/
      {paper_slug}/
        paper.pdf
        note.md
        metadata.json
        parsed/
          raw.md
          refined.md
          sections/
            01_intro.md
            02_method.md
            ...
        extracted/
          knowledge.json
          datasets.json
        jobs/
          history.json
```

说明：

- `metadata.json` 为文件侧缓存，不替代数据库
- 人工审查直接复用 `parsed/refined.md`，不额外创建 `review.md`
- `note.md` 是统一研究笔记，不拆出独立的 human note

### 5.3 Project 目录布局

课题统一落在 `Projects/` 顶层目录下，按 `ProjectName` 组织，落盘时使用 slug 化路径。

建议目录结构：

```text
Projects/
  {project_slug}/
    overview.md
    related-work.md
    method.md
    experiment.md
    conclusion.md
    manuscript.md
    assets/
    exports/
    jobs/
```

### 5.4 导出物布局

导出物进入各自资源目录，不集中放到单独的全局导出目录。

例如：

- 论文相关导出物放在对应 `Papers/.../{PaperName}/`
- 课题相关导出物放在 `Projects/{ProjectName}/exports/`
- 未来汇报导出物放在对应 `Presentation` 所属目录

### 5.5 数据库存储内容

SQLite 主要保存：

- 资源主键与元数据
- 文件逻辑路径与状态
- 资源之间的显式关系
- 异步任务状态与依赖
- LLM / Agent 配置
- Prompt 模板
- 探活与观测结果

---

## 6. 核心业务链路

### 6.1 Paper 主链路

`Paper` 是当前最优先落地的主链路：

1. 用户通过 DOI、URL 或标题创建论文记录
2. 系统创建 `download` 任务，获取或挂接 `paper.pdf`
3. 系统创建 `parse` 任务，生成 `parsed/raw.md`
4. 系统创建 `refine` 任务，生成 `parsed/refined.md`
5. 系统进入人工审查节点，等待用户修改并确认
6. 用户确认后，系统创建 `split` 任务，生成章节结果
7. 系统创建 `analyze` / `note_generate` 任务
8. 系统更新 `note.md`
9. 系统触发 `Knowledge` 与 `Dataset` 抽取

### 6.2 Project 链路

`Project` 在 `Paper` 主链路稳定后接入：

1. 用户创建课题
2. 系统初始化六个 Markdown 模块
3. 用户关联论文并标注关系类型
4. 用户手动触发 related work、method、experiment 等模块任务
5. 生成结果写回对应模块文件

### 6.3 Knowledge / Dataset 链路

`Knowledge` 与 `Dataset` 在当前阶段以“从论文链路中抽取并落库”为主：

- `Knowledge`：抽取观点、定义、特征及证据片段
- `Dataset`：抽取数据集名称、任务类型、评价方式、使用论文

当前阶段不做复杂工作台，只保证：

- 可稳定抽取
- 可稳定落库
- 可与论文、课题建立关系

`Knowledge` 的首版字段应尽量完整，至少覆盖：

- 中文总结
- 英文原文
- 知识类型，如定义 / 观点 / 特征
- 所属类别或研究方向
- 来源论文与证据位置
- 抽取置信度
- 后续人工修订状态

### 6.4 Presentation 链路

`Presentation` 在当前首版中需要实现，但优先级低于 `Paper` 与 `Project`：

- 允许从 `Project` 创建 Presentation 资源
- 允许记录输入材料与导出目标
- 允许生成中间草稿与导出结果
- 当前阶段可先实现较简化但完整可用的生成链路

---

## 7. 任务系统设计

### 7.1 设计目标

任务系统需要同时支持：

- 多个同类型任务并发执行
- 多个不同类型任务并发执行
- 同一资源上的任务按依赖顺序串行推进
- 人工审查节点暂停与恢复

典型场景：

- 多篇论文同时下载 PDF
- 一篇论文在下载，另一篇论文在解析
- 多篇论文同时进行不同阶段处理
- 同一篇论文不能同时执行冲突步骤

### 7.2 任务模型

建议将任务拆成三个概念：

- `JobDefinition`：任务类型定义，例如 `download_pdf`、`parse_pdf`
- `JobInstance`：某次具体执行，例如“对 paper_A 执行下载”
- `WorkflowCheckpoint`：流程中的暂停点，例如人工审查待确认

### 7.3 并发原则

并发控制建议遵循以下规则：

- 不同资源上的任务允许并发
- 同类型任务在不同资源上允许并发
- 不同类型任务在不同资源上允许并发
- 同一资源上的冲突任务必须串行

示例：

- `paper_A.download_pdf` 与 `paper_B.download_pdf` 可以并发
- `paper_A.download_pdf` 与 `paper_B.parse_pdf` 可以并发
- `paper_A.parse_pdf` 与 `paper_A.refine_parse` 不能无依赖并发
- `paper_A.refine_parse` 与 `paper_A.note_generate` 不能直接并发

### 7.4 资源级锁

为了实现“跨资源并发、单资源有序”，任务系统应引入资源级锁或资源级串行队列。

建议引入：

- `resource_key`：如 `paper:{paper_id}`
- `job_stage`：如 `download`、`parse`、`refine`、`review`、`split`
- `dependency_job_id` 或阶段依赖规则

约束：

- 同一 `resource_key` 上只允许一个互斥阶段任务处于 `running`
- 非互斥的只读任务未来可放宽
- 当前阶段先采用保守策略，即同一论文主链路任务单线程推进

### 7.5 SQLite 下的任务协调策略

由于当前阶段采用 `SQLite`，需要避免多 worker 高频写入导致数据库锁竞争。

当前推荐策略：

- `FastAPI` 负责接收请求、创建任务
- 单独的任务调度器负责从 `biz_job` 拉取待执行任务
- 调度器内部可使用有限并发执行器处理 I/O 型任务
- 任务状态写回由调度器统一协调，避免多写者混战

建议首版采用：

- 单调度器进程
- 有限并发 worker 池
- 数据库写操作串行化

这样可以兼顾：

- 多 PDF 下载并发
- 多资源任务并发
- SQLite 可控的锁竞争

后续若迁移到 `PostgreSQL`，可以再升级为更强的多 worker 模式。

### 7.6 任务状态机

建议统一状态：

- `queued`
- `running`
- `waiting_review`
- `waiting_confirm`
- `succeeded`
- `failed`
- `cancelled`

其中：

- `waiting_review`：系统已生成待审查文档，等待用户修改
- `waiting_confirm`：用户修改完成但尚未确认继续

---

## 8. 人工审查与确认机制

### 8.1 节点位置

人工审查节点位于：

`LLM 优化解析` 之后，`章节切分` 之前

### 8.2 工作方式

当前方案不是“纯暂停不产物”，而是：

1. 系统完成 `refined.md`
2. 用户直接在 `parsed/refined.md` 上进行人工修改
3. 任务状态进入 `waiting_review`
4. 用户完成修改后点击确认
5. 系统创建或恢复后续任务，进入 `split`

### 8.3 与“自动继续”的区别

自动继续模式的特点是：

- 无人工检查
- `refined.md` 直接进入后续切分
- 速度快，但不可控

人工审查确认模式的特点是：

- 用户会修改中间文档
- 后续步骤必须以确认后的版本为输入
- 更适合首版对解析质量进行兜底

因此当前架构应显式支持“审查暂停 -> 用户修改 -> 用户确认 -> 恢复流程”。

---

## 9. note 增量更新策略

### 9.1 单文档原则

论文级研究笔记统一为单个 `note.md`，系统生成与人工修改都发生在同一文档中。

### 9.2 重生成策略

当前确定采用“合并式增量更新”，不采用无提示覆盖。

目标是：

- 保留用户已经修改的内容
- 仅更新与新分析结果强相关的区块
- 记录最近一次自动生成与人工修改状态
- 能够准确识别当前 `note.md` 是否已被用户修改、是否已合并、是否存在冲突待处理

### 9.3 建议机制

首版建议采用“区块化合并”策略：

- 为系统生成区块设置稳定标记
- 人工自由补充区块默认不自动覆盖
- 再次生成时只更新系统托管区块

例如：

- `Research Question`
- `Core Method`
- `Main Contributions`
- `Experiment Summary`
- `Limitations`

每个区块可保留：

- `generated_at`
- `source_sections`
- `last_merged_at`

建议同时维护 `note_status` 与内容指纹：

- `clean_generated`：纯系统生成，用户未修改
- `user_modified`：检测到用户修改
- `merged`：系统已完成一次增量合并
- `conflict_pending`：存在无法自动安全合并的区块

识别方式建议结合：

- 文档整体 hash
- 系统托管区块 hash
- 最近一次自动生成时间
- 最近一次用户确认时间

这样既能增量更新，也能避免整篇笔记被完全重写。

---

## 10. LLM / Agent 架构

### 10.1 总体原则

- 保留通用 LLM 抽象
- provider 通过协议接入
- `Agno` 作为统一的 LLM / Agent 管理框架
- 业务流程只依赖统一能力接口，不依赖具体平台 SDK
- 高层任务场景可抽象为 skill，并通过 `Agno + provider + prompt + tools` 执行

### 10.2 provider 协议

建议定义统一协议：

```python
class LlmProvider(Protocol):
    def chat(self, request: ChatRequest) -> ChatResponse:
        ...

    def stream(self, request: ChatRequest) -> Iterator[ChatDelta]:
        ...

    def probe(self, request: ProbeRequest) -> ProbeResult:
        ...
```

建议 provider registry 根据配置动态装配：

- `openai`
- `deepseek`
- `siliconflow`
- 其他兼容 provider

### 10.3 Agno 的位置

`Agno` 用于承载：

- 任务场景到 agent 的绑定
- skill 到 agent / prompt / tool 的绑定
- prompt 与工具的组织方式
- 不同任务的 agent profile 运行入口

当前阶段不做复杂多 agent 编排中心，只做：

- 场景级 agent 配置
- skill 级任务封装
- provider + model + prompt 绑定
- 可替换的 agent runtime

其中 `Knowledge` 抽取尤其适合做成 skill 化能力：

- 输入：章节化论文内容、元数据、抽取目标
- 输出：结构化 Knowledge 条目
- 执行器：`Agno` 管理的 LLM workflow
- 结果：写回数据库并建立来源关系

### 10.4 配置粒度

配置按“处理环节”组织，而不是按页面组织。

至少覆盖：

- 论文优化解析
- 分节后分析
- `note` 生成
- Knowledge 抽取
- Dataset 抽取
- related work 整理
- method 草稿生成
- experiment 建议
- probe 探活

### 10.5 兜底策略

每个关键环节配置：

- 主 provider / model
- 备用 provider / model
- 超时与重试策略

运行时优先使用主模型，失败时根据策略切换备用模型。

---

## 11. 模块划分

### 11.1 文献管理模块

职责：

- 创建论文记录
- 下载 / 挂接 PDF
- 原始解析
- 优化解析
- 人工审查
- 分节
- `note` 生成

### 11.2 课题管理模块

职责：

- 创建 `Project`
- 初始化六模块
- 关联论文
- 触发 related work、method、experiment、conclusion 等任务

### 11.3 Knowledge / Dataset 模块

职责：

- 从论文分析结果中抽取结构化内容
- 落库并建立来源关系
- 提供基础查询与关系浏览能力
- 将 `Knowledge` 抽取作为 skill 化 LLM 能力管理

### 11.4 配置中心模块

职责：

- 管理 provider、model、agent profile、prompt template
- 探测联通状态与首字时延
- 统一为任务系统提供运行配置

### 11.5 任务系统模块

职责：

- 维护任务队列
- 执行任务
- 管理依赖、锁、失败重试和人工确认节点

### 11.6 Presentation / Feed / Conference 模块

职责：

- 当前阶段需要进入首版实现范围
- 实现顺序后置，但应形成最小可用闭环
- 可以先提供简化实现，再逐步增强交互和自动化深度

---

## 12. 当前阶段实施顺序

建议实施顺序与路线图保持一致：

### P0

- `Paper` 主链路
- 任务系统基础能力
- 人工审查确认机制
- 配置中心与 provider 抽象
- `Knowledge` / `Dataset` 基础落库

### P1

- `Project` 模块深化
- `Knowledge` 与 `Dataset` 基础查询能力
- `Presentation`
- 每日推荐
- 会议追踪

### P2

- 推荐
- 图谱增强交互
- 投稿 / Rebuttal
- 录用后处理
- Zotero 集成

---

## 13. 与其他文档的关系

本文件是后端总体技术方案，细节继续下沉到以下文档：

- `数据模型.md`：定义资源对象边界
- `表设计.md`：定义表结构与关系
- `modules/*.md`：定义单模块流程和职责
- `03_api/接口规范.md` 与 `endpoints/*.md`：定义接口契约
- `services/*.md`：定义外部服务接入方式

换句话说：

- 本文档回答“整体怎么搭”
- 数据模型回答“对象怎么建”
- 表设计回答“表怎么落”
- 模块文档回答“模块怎么跑”
- API 文档回答“前后端怎么接”

---

## 14. 当前待确认项

以下设计点已经有初步方向，但仍建议在实现前进一步确认：

- `note.md` 的区块标记格式与合并规则
- 首版任务执行器采用线程池、协程池还是混合模式
- `Presentation` 首版最小可用闭环需要暴露到什么程度的 API

---

*文档版本：v0.1 | 最后更新：2026-04-23*
