# =============================================================================
# Research-Flow backend/.env 示例
# 用法：
# 1. 复制为 backend/.env
# 2. 这里只放“本机覆盖项”和“敏感信息”
# 3. 通用默认值统一放在 backend/config/settings.toml
# 4. 不要把真实 backend/.env 提交到仓库
# =============================================================================

# -----------------------------------------------------------------------------
# Zotero MCP 敏感/本机专属配置
# 这些值通常不应进入仓库。
# -----------------------------------------------------------------------------
ZOTERO_API_KEY=
ZOTERO_LIBRARY_ID=
UNPAYWALL_EMAIL=
UNSAFE_OPERATIONS=

# -----------------------------------------------------------------------------
# 本机覆盖项（可选）
# 只有当本机配置不同于 backend/config/settings.toml 时才新增对应键。
# 示例：ZOTERO_MCP_COMMAND=C:\path\to\zotero-mcp.exe
# -----------------------------------------------------------------------------
