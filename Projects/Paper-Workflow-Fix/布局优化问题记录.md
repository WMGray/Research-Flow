---
title: 今日待做 - Workflow 与首页布局优化
updated: 2026-05-15 20:51:32
status: todo
tags:
  - frontend
  - workflow
  - layout
---

# 今日目标

- [ ] 在浏览器 100% 缩放下压缩首页和 Workflow 的尺寸体系，让核心信息尽量首屏可见。
- [ ] 统一用户可见描述性文案为中文，保留论文标题、会议名和必要技术术语英文。
- [ ] 将 Workflow 检索视图和获取视图改成更紧凑的工作台布局。

# 现状记录

- 100% 缩放检查结果：`visualViewport.scale = 1`，桌面视口约 `2049 x 831`。
- 首页当前 `scrollHeight ≈ 1736px`，首屏只能看到 hero 和第一排卡片上半部分。
- Workflow 检索视图当前 `scrollHeight ≈ 4488px`，首屏只能看到 `Search Batches` 表格前几行，`Candidate Screening` 完全在首屏外。
- Workflow 获取视图当前 `scrollHeight ≈ 917px`，首屏能看到 4 条记录，但顶部指标区和表格工具栏仍偏高。
- 仍有大量英文 UI 文案残留：`Workflow Dashboard`、`Search`、`Acquire`、`Library`、`Discovery Workflow`、`Search Batches`、`Candidates`、`Gate 1 Keep`、`Keep`、`Reject`、`Coming Soon` 等。

# 待办清单

## P0-1. 统一中文化文案

- [ ] 将导航、tab、按钮、卡片标题、表头和说明性文案统一改为中文。
- [ ] 建议映射：
  - `Discover` / `Search` -> `检索`
  - `Acquire` -> `获取`
  - `Library` -> `文库`
  - `Workflow Dashboard` -> `流程看板`
  - `Discovery Workflow` -> `检索流程`
  - `Search Batches` -> `检索批次`
  - `Candidates` -> `候选论文`
  - `Gate 1 Keep` -> `初筛保留`
  - `Acquire Queue` -> `获取队列`
  - `Keep` -> `保留`
  - `Reject` -> `剔除`
  - `Coming Soon` -> `即将开放`
- [ ] 将长英文说明压缩为中文短句，例如 Workflow hero 的说明不再使用长句。

## P0-2. 压缩 Workflow 顶部结构

- [ ] 将页面标题、说明文案和 `检索 / 获取 / 文库` 视图切换合并到更紧凑的顶部区域。
- [ ] 检索视图建议取消或极度压缩 hero：将图标、kicker、标题和说明合并为一条工作流提示栏。
- [ ] 获取视图将标题、说明、搜索框和 stage tabs 合并为一行或两行紧凑工具栏。
- [ ] 减少顶部纵向堆叠，避免 `header + hero + metric` 连续占用首屏。

## P0-3. 压缩 Workflow 指标卡片

- [ ] 将四个指标卡片从当前大卡片改为紧凑 KPI 卡片。
- [ ] 目标高度从当前约 `161px` 压到 `72-96px`。
- [ ] 压缩卡片 padding、图标尺寸、数字字号、说明文字字号和卡片间距。
- [ ] 检索视图与获取视图都保持桌面四列一行展示。

## P1-1. 压缩首页 hero

- [ ] 首页 hero 当前约 `373px` 高，目标压到 `220-260px`。
- [ ] 缩小标题字号、说明文字、按钮高度和左右内边距。
- [ ] 右侧插画缩小或弱化，避免占用过多首屏高度。
- [ ] `打开 Workflow`、`打开获取队列` 按钮改为更紧凑的操作区。

## P1-2. 重排首页概览模块

- [ ] 首页“论文流程概览”当前 7 个 stat tile 高度过大，需改为更紧凑的 KPI 条目。
- [ ] 优先方案：改为一行或两行小指标，不再使用大卡片套小卡片。
- [ ] 压缩“研究快照”和“会议信号”的面板高度，减少不必要留白。
- [ ] 目标是在 100% 缩放下尽量展示 hero、流程概览、研究快照和会议信号。

## P1-3. 压缩表格和列表密度

- [ ] Workflow `Search Batches` 表格行高可继续压缩，首屏应显示更多批次。
- [ ] `Candidate Screening` 的筛选栏和候选表格行高需要降低。
- [ ] 获取视图表格行高当前约 `92px`，可在不牺牲可读性的前提下继续压缩。
- [ ] 按钮组和 pipeline badges 改为更紧凑尺寸。

# 验收标准

- [ ] 浏览器 100% 缩放下，首页首屏尽量展示 hero、流程概览、研究快照和会议信号等主要模块。
- [ ] Workflow 检索视图顶部明显变矮，`Search Batches` 能展示更多内容，`Candidate Screening` 更早进入视野。
- [ ] Workflow 获取视图首屏保留表格主体，并减少顶部指标区和工具栏高度。
- [ ] 四个 Workflow 指标卡片在桌面宽度下一行展示，高度明显低于当前版本。
- [ ] 用户可见描述性文案统一为中文，不再出现大面积英文 UI 文案。
- [ ] 页面在 100% 缩放下无文字重叠、按钮溢出或布局横向滚动。

# 备注

- 论文标题、会议名、数据源标识和必要技术术语可保留英文。
- `Coming Soon` 功能仍保持禁用状态，只调整中文文案和视觉尺寸。
- 本轮先记录 TODO，不执行代码改动。
